# Old Experiments

This folder contains models from failed experiments:

- `contrastive_model.pt` - Deep learning contrastive model (MRR=0.0012, failed)
- `job_embeddings.pkl` - TF-IDF embeddings experiment (MRR=0.0069, worse than baseline)

**Current production model:**
- `../cooccurrence_model.pkl` - Co-occurrence model (MRR=0.0345, Score=0.2752)
